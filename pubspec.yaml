name: my_telco
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.1.0
  equatable: ^2.0.5
  bloc: ^8.1.4
  get_it: ^8.2.0
  flutter_bloc: ^8.1.5
  dartz: ^0.10.1
  connectivity_plus: ^6.1.4
  http: ^1.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/nav_bar/
    - assets/history/
    - assets/mock_data/
